# MCard Configuration

# Database configuration
# The paths are relative to the project root directory
MCARD_STORE_PATH=data/mcard.db
MCARD_STORE_MAX_CONNECTIONS=5
MCARD_STORE_TIMEOUT=30.0

# Data source for loading files
MCARD_DATA_SOURCE=data/source

# Hash function configuration
# Options: sha256, sha512, sha1, md5, custom
MCARD_HASH_ALGORITHM=sha256

# Custom hash function configuration (only needed if MCARD_HASH_ALGORITHM=custom)
# MCARD_HASH_CUSTOM_MODULE=mypackage.hashing
# MCARD_HASH_CUSTOM_FUNCTION=my_hash_function
# MCARD_HASH_CUSTOM_LENGTH=32
