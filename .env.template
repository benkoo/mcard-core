# MCard Configuration

# Database paths
MCARD_DB=data/mcard.db
MCARD_TEST_DB=data/test_mcard.db

# Data source for loading files
MCARD_DATA_SOURCE=data/source

# Hash function configuration
# Options: sha256, sha512, sha1, md5, custom
MCARD_HASH_FUNCTION=sha256

# Custom hash function configuration (only needed if MCARD_HASH_FUNCTION=custom)
# MCARD_CUSTOM_HASH_MODULE=mypackage.hashing
# MCARD_CUSTOM_HASH_FUNCTION=my_hash_function
# MCARD_CUSTOM_HASH_LENGTH=32
